/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['img.clerk.com','images.clerk.dev','ssa-blogs.s3.us-east-1.amazonaws.com'],
    },
  };
  
  export default nextConfig;
  